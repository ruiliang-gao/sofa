# CMake package configuration file for the cudpp library

@PACKAGE_INIT@

if(NOT TARGET cudpp)
	include("${CMAKE_CURRENT_LIST_DIR}/cudppTargets.cmake")
endif()

check_required_components(cudpp)

set(cudpp_LIBRARIES cudpp)
set(cudpp_INCLUDE_DIRS @PACKAGE_CUDPP_INCLUDE_DIR@)
