# CMake package configuration file for the ZyWorkerThreads library

@PACKAGE_INIT@
find_package(SofaMisc REQUIRED)
find_package(Boost COMPONENTS system chrono thread REQUIRED)
if(NOT TARGET ZyWorkerThreads)
	include("${CMAKE_CURRENT_LIST_DIR}/ZyWorkerThreadsTargets.cmake")
endif()

check_required_components(ZyWorkerThreads)
