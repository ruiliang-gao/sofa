# CMake package configuration file for the Simulation plugin

@PACKAGE_INIT@
if(NOT TARGET Simulation)
	include("${CMAKE_CURRENT_LIST_DIR}/Simulation.cmake")
endif()

check_required_components(Simulation)
