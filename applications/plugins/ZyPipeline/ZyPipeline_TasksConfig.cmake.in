# CMake package configuration file for the ZyPipeline_Tasks library

@PACKAGE_INIT@
find_package(SofaMisc REQUIRED)
find_package(Boost COMPONENTS system chrono thread REQUIRED)
if(NOT TARGET ZyPipeline_Tasks)
    include("${CMAKE_CURRENT_LIST_DIR}/ZyPipeline_TasksTargets.cmake")
endif()

check_required_components(ZyPipeline_Tasks)
