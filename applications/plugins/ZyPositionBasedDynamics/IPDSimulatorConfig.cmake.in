# CMake package configuration file for the ZyPositionBasedDynamics plugin

@PACKAGE_INIT@
if(NOT TARGET IPDSimulation)
	include("${CMAKE_CURRENT_LIST_DIR}/IPDSimulation.cmake")
endif()

check_required_components(IPDSimulation)
