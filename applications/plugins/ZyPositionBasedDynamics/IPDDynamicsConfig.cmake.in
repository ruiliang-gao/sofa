# CMake package configuration file for the ZyPositionBasedDynamics plugin

@PACKAGE_INIT@
if(NOT TARGET IPDDynamics)
	include("${CMAKE_CURRENT_LIST_DIR}/IPDDynamics.cmake")
endif()

check_required_components(IPDDynamics)
